export type SshHost = { id: string; revision: number; name: string; hostname: string; port: number; username: string; root: string; auth: 'agent' | 'password' | 'privateKey'; keyPath: string; fingerprint?: string; hasSecret?: boolean; jumpHostId?: string };
export type SshChallenge = { hostId: string; revision: number; fingerprint: string; previousFingerprint?: string; expiresAt: number };
export type SshSession = { sessionId: string; hostId: string; name: string; hostname: string; cwd: string; status: 'connecting' | 'ready' | 'failed' | 'closed' | 'disconnected' | 'exited'; cols: number; rows: number; inputSeq: number; exitCode: number | null; error: string };
export type SshRead = SshSession & { data: string; cursor: number; truncated: boolean; hasMore: boolean };
export type SshTransfer = { transferId: string; sessionId: string; direction: 'upload' | 'download'; name: string; remotePath: string; localPath: string; bytesTotal: number; bytesDone: number; status: 'running' | 'completed' | 'failed' | 'canceled' | 'detached'; stage: 'preparing' | 'copying' | 'verifying' | 'publishing'; error: string; sha256?: string; startedAt: number; finishedAt: number; stats: { maxInFlightBytes: number } };
export type SshForward = { forwardId: string; sessionId: string; hostId: string; remoteHost: string; remotePort: number; localPort: number; status: 'starting' | 'active' | 'closed' | 'failed' | 'detached'; connectionsServed: number; connectionsActive: number; error: string; startedAt: number; closedAt?: number };
